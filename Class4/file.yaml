---
# connection block
- name: Test playbook to run
  hosts: all
  user: ec2-user
  become: true
  become_method: sudo 
  tasks:
  - name: Change file ownership, group and permissions
    ansible.builtin.file:
      path: /etc/foo.conf
      owner: ec2-user
      group: ec2-user
      mode: '0644'

# ansible-playbook -i ../setup/inventory   file.yaml 